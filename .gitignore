# Secrets - never commit
.env

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Generated demo output
demo_tearsheet.html

# OS
.DS_Store

# Agent state lives on the `agent-state` branch, shared between the cloud and
# PC runners by scheduler/shared_state.py (which adds these with -f). Ignored
# here so they never get committed to main by accident.
.state/
.cache/
trade_log.csv
TRADE_LOG.md
TRIAL_REPORT.md
/reports/
logs/
/KILL_SWITCH
